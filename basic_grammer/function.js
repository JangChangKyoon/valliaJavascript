function plus(a, b) {
  return a + b;
}

console.log(plus(1, 2));


//object 안 함수 만들기
const calculator

//--------------------
const calculator = {
  plus: function (a, b) {
    return a + b;
  },
};

const plusResult = calculator.plus(2, 3);
